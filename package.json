{
  "name": "liang-art-studio",
  "version": "0.9.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "cms:deploy": "sanity deploy",
    "migrate:legacy": "sanity exec scripts/migrate-legacy-gallery.mjs --with-user-token --",
    "seed:content": "sanity exec scripts/seed-content.mjs --with-user-token",
    "seed:demo": "sanity exec scripts/seed-demo.mjs --with-user-token",
    "remove:demo": "sanity exec scripts/remove-demo.mjs --with-user-token",
    "migrate:legacy:check": "sanity exec scripts/migrate-legacy-gallery.mjs --with-user-token -- --dry-run",
    "cms:dev": "sanity dev"
  },
  "dependencies": {
    "@sanity/client": "^8.6.1",
    "@sanity/image-url": "^2.1.1",
    "astro": "^7.3.2",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "sanity": "^6.13.0",
    "styled-components": "^6.5.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^5.9.2",
    "vite": "^8.2.2",
    "wrangler": "^4.131.0"
  },
  "engines": {
    "node": ">=22.12"
  }
}
